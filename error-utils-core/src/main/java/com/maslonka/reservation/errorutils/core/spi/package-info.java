/**
 * Service-provider interfaces for pluggable infrastructure behaviour.
 *
 * <p>Implementations of these interfaces can be registered as Spring beans to override the
 * default behaviour provided by the starter. The auto-configuration registers default
 * implementations only when no bean of the matching type is already present.</p>
 */
@NullMarked
package com.maslonka.reservation.errorutils.core.spi;

import org.jspecify.annotations.NullMarked;
